filebeat.modules:
  - module: system
    syslog:
      enabled: false
    auth:
      enabled: true
      var.paths: ["/logs/auth.log*"]

setup.kibana:
  host: "kibana:5601"

output.logstash:
  hosts: ["logstash:5044"]
